# Masterboard runs fully client-side; there are no build-time secrets.
# Per-GM runtime config (GitHub PAT, repo, Imgur client id, source-site URLs)
# is entered in the app's Settings and stored encrypted in IndexedDB — never here,
# never committed. This file documents that there is intentionally nothing to set.
